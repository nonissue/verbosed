# Migration `20201129045615-move-to-postgres`

This migration has been generated by nonissue at 11/28/2020, 9:56:16 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "words" (
"id" SERIAL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "title" TEXT NOT NULL,
    "definition" TEXT NOT NULL,
    "published" BOOLEAN NOT NULL DEFAULT true,
    "published_date" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "words.title_unique" ON "words"("title")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201127231345-publish----published..20201129045615-move-to-postgres
--- datamodel.dml
+++ datamodel.dml
@@ -1,23 +1,20 @@
-// This is your Prisma schema file,
-// learn more about it in the docs: https://pris.ly/d/prisma-schema
+generator client {
+  provider = "prisma-client-js"
+}
 datasource db {
-  provider = "sqlite"
-  url = "***"
+  provider = "postgresql"
+  url = "***"
 }
-generator client {
-  provider = "prisma-client-js"
-}
-
 model Word {
   id            Int      @id @default(autoincrement())
-  createdAt     DateTime @default(now()) @map(name: "created_at")
-  updatedAt     DateTime @default(now()) @map(name: "updated_at")
+  createdAt     DateTime @default(now()) @map("created_at")
+  updatedAt     DateTime @default(now()) @map("updated_at")
   title         String   @unique
   definition    String
   published     Boolean  @default(true)
-  publishedDate DateTime @default(now()) @map(name: "published_date")
+  publishedDate DateTime @default(now()) @map("published_date")
-  @@map(name: "words")
+  @@map("words")
 }
```


